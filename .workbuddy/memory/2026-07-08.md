# 2026-07-08

- 写了一篇博客 `source/_posts/2026/07/101513.md`：真实根因是查询条件退化为只有 `is_deleted = 0`（查询参数为空时 SQL 全表扫描）导致 OOM。"校验参数不能为空"的目的就是防止查询条件坍塌成全表扫描。三层防御：参数校验 + 过滤字段复合索引 + 分页/行数上限。
- 写了第二篇博客 `source/_posts/2026/07/101937.md`：基于用户草稿整理成正式文章——`PdfCheckUtil.java` 中 `PdfReader` 创建后未 `close()` 导致文件句柄泄漏，表现为 `lsof +L1` 大量 (deleted) PDF 撑满磁盘。根因是 unlink 只删目录项、FD 不关空间不回收；修复为两个方法都补 `finally` 关闭。
- 用户反馈两篇博客"太 AI 话、不够专业"。已将两篇改为 incident report / 技术笔记风格：去掉说教与鸡汤结尾，保留术语精确性与直接陈述；结论段改为技术要点总结而非人生感悟。
- 已 git commit 并 push 到 origin/main（commit 31a648ab）。提交范围：两篇 7 月新文 + 两篇 6 月未提交文章（source/_posts/2026/06/*）+ AGENTS.md 微调。刻意排除 `.workbuddy/`（含本地记忆，不应进公开仓库）。git 身份沿用历史实际使用的 cong <chusnow@outlook.com>（AGENTS.md 里的 qbmzc 是模板默认值，与实际不符）。